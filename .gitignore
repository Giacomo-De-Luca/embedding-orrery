# Claude Code
.claude/

# Submodules (reference only, not tracked)
neuronpedia/

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Exception: forked plotly.js needs its build/ dir (contains generated plotcss.js)
!embedding_visualization/forked/plotly.js/build/
!embedding_visualization/forked/plotly.js/build/**

## Random Test Files and notebooks
test*.py
test*.ipynb
# ...but real unit tests are tracked (without this, new test files in
# unit_tests/ are silently ignored and never make it into commits)
!interpretability_backend/unit_tests/test*.py
.env

## test datasets
interpretability_backend/resources/uploads/*
interpretability_backend/resources/psycolinguistics/*
interpretability_backend/resources/main.duckdb
interpretability_backend/resources/main.duckdb.*
interpretability_backend/resources/sae_labels/*
interpretability_backend/resources/sae_vectors/*
interpretability_backend/resources/seed/*
interpretability_backend/resources/probing_results/*
interpretability_backend/resources/acl_anthology/
interpretability_backend/resources/datasets/

### interrupted progress

*/job_state.json

job_state.json


## Lacan Dataset:

*.pkl
*.pickle
*.npy
seminars.pdf
seminars.txt
writings.pdf
writings.txt
sentence_list.tsv
nrc_annotated.tsv
lacan_sentences.tsv


# Virtual environments
.venv

Papers/
vector_db/

# Shipped seed snapshot (small demo collections, ~24 MB) — tracked.
# Negations must follow the broad `vector_db/` rule above to take effect.
!interpretability_backend/resources/seed/
!interpretability_backend/resources/seed/main.duckdb
!interpretability_backend/resources/seed/vector_db/
!interpretability_backend/resources/seed/vector_db/**

# HF Space demo seed (~313 MB, adds the EMNLP collection) — NEVER tracked:
# its single HNSW file exceeds GitHub's 100 MB hard limit. Ships only in the
# Space repo via deploy/hf-space/deploy.py (auto-LFS). See HF_SPACE_DEMO.md.
interpretability_backend/resources/seed_demo/


interpretability/resources
embedding-atlas/
tensorboard/
embedding_visualization/vector_db
square-ui/
sentence-transformers/
projecting_sentences/
references/

## embedding interpretability experiments
interpretability/results/

# embedding_visualization (Next.js) specific
embedding_visualization/node_modules/
embedding_visualization/.next/
embedding_visualization/out/
embedding_visualization/build/
embedding_visualization/.vercel/
embedding_visualization/*.tsbuildinfo
embedding_visualization/.env*
!embedding_visualization/.env.example

# Projection data (now stored in ChromaDB, no need for JSON files)
embedding_visualization/public/data/*.json
!embedding_visualization/public/data/collections.json

# General patterns (apply everywhere)
node_modules/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

interpretability/results/

wordnet_senses.tsv

# Local-only experiment (kept on disk, not tracked)
interpretability_backend/interpret/experiments/poetry_directions/